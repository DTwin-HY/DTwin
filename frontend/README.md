# Frontend
This directory contains the frontend component of the project. This directory contains the formatting and linting configurations, the deployment Dockerfile, the Nginx configuration (for deployment only), and the Vite configuration file.

## Subdirectories
* [src](./src/) contains the React components and css files.
* [public](./public/) contains all the assets.